import styled from "styled-components";

const FooterWrapper = styled.div`
width: 100%;
`

export default function Footer() {
    return (
        <FooterWrapper>
            Footer
        </FooterWrapper>
    )
}