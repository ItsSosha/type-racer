import styled from "styled-components";

const StyledMain = styled.main`
align-self: center;
margin-top: auto;
margin-bottom: auto;
`

export default function Main() {
    return (
        <StyledMain>
            <div>
                Main
            </div>
        </StyledMain>
    )
}